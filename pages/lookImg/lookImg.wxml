<view class="back" bindtap='back'>
  <image src="../../image/down.png" class='zhankai'></image>
</view>
<view class="lookPdf">
  <view style="margin-top:50rpx" wx:for="{{imgContent}}" wx:key="{{item}}">
    <image src="{{item}}"></image>
  </view>
</view>