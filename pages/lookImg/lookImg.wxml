<!--pages/detail/lookPdf/lookPdf.wxml-->
<view class="lookPdf">
  <view class="title">{{imgTitle}}</view>
  <view style="margin-top:34rpx;font-size:26rpx;">{{imgTime}}</view>
  <view style="margin-top:50rpx" wx:for="{{imgContent}}" wx:key="{{item}}">
    <image src="{{item}}"></image>
  </view>
</view>